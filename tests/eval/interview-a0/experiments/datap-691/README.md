# DATAP-691 — live model-run interview (gpt-5.6-luna, skill pin 2616ebc)

**Ticket:** [DATAP-691](https://dataparade.atlassian.net/browse/DATAP-691)  
**Parent:** [DATAP-669](https://dataparade.atlassian.net/browse/DATAP-669)  
**Prior runs:** [DATAP-676](../datap-676/) (skill `05a6a5b…`); frozen — do not edit.

## Pins

| Pin | Value |
|-----|-------|
| Model | `gpt-5.6-luna` (`api_response.model`) |
| Skill | `2616ebccb4f424bd4281ae07e1bb94046b66f41a` (`run_meta.skillSha`) |
| Brief | `16f2e857a47d54bfea6ca5d7f23a6c4f2732da29` (unchanged) |
| Ontology | `0.2.0` @ `0656c5d9a6ce0d31440c63327ce597ce8df4414f` |

Fixtures loaded from `tests/eval/interview-a0/fixtures/dpkb/` at the pins above.

## Artifacts

| File | Role |
|------|------|
| `raw-model-transcript.jsonl` | **Primary evidence** — unedited OpenAI request/response log |
| `map-raw-to-actions.ts` | Mechanical `ACTION_JSON:` → harness actions |
| `mapped-transcript.json` | Mapper output (regenerated by score test) |
| `score-report.json` | Harness score on six fail-any rubric lines |
| `discovery-export-bundle.json` | `exportInterviewDiscoveries` output (`source=interview`, not landable) |
| `stakeholder-script.yaml` | Scripted stakeholder for unknown rows only |

## Re-run

```bash
# Live model run (OPENAI_API_KEY required)
pnpm run eval:interview-a0:run-model-luna-691

# Map + score + export bundle (CI-safe)
pnpm run eval:interview-a0:datap-691
```

**Not a living-KB land.** Adapter export only; no auto-promotion. Frozen experiments `datap-673/674/676` untouched.

## Score (2026-09-16 live Luna run)

| Rubric line | Result |
|-------------|--------|
| 1. re_ask_known | pass |
| 2. refuse_vs_invent | pass |
| 3. no_mush_merge | pass |
| 4. taxonomy_discipline | **fail** (model wrote `unspecified` on `data_categories`) |
| 5. edge_mode | pass |
| 6. provenance | pass |

**Overall:** fail (taxonomy). Harness + `exportInterviewDiscoveries` still run on the frozen raw log. See `score-report.json` and `discovery-export-bundle.json` (DATAP-682 shapes: D2=in_scope, D4=actor_kind, D7=data_categories, D8=purpose; invalid category writes refused).
