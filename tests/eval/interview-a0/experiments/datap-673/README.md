# DATAP-673 — live model-run interview experiment

**Ticket:** [DATAP-673](https://dataparade.atlassian.net/browse/DATAP-673)  
**Not** the hand-authored `a0-passing-simulated` case in `cases.ts`.

## What ran

Live OpenAI interview (`gpt-4o-mini`, temperature 0) against pinned fixtures:

- `fixtures/dpkb/a0-dataflow-interview/SKILL.md` @ `8175d44…`
- `fixtures/dpkb/dogfood-brief-a0.md` @ `16f2e85…`

Stakeholder replies are **scripted** (`stakeholder-script.yaml`) and constrained to brief unknown rows only. The interviewer model output is the evidence.

## Artifacts (primary → derived)

| File | Role |
|------|------|
| `raw-model-transcript.jsonl` | **Primary evidence** — unedited OpenAI request/response log |
| `map-raw-to-actions.ts` | Mechanical converter: `ACTION_JSON:` lines → harness actions |
| `mapped-transcript.json` | Mapper output only (regenerated by score test) |
| `score-report.json` | Harness score on six fail-any rubric lines |
| `stakeholder-script.yaml` | Scripted stakeholder (not the model) |

## Re-run pipeline

```bash
# 1. Live model run (requires OPENAI_API_KEY)
pnpm run eval:interview-a0:run-model

# 2. Map raw log → score (no API key; uses checked-in raw log in CI)
pnpm run eval:interview-a0:datap-673
```

Mapper unit tests: `pnpm exec jest tests/eval/interview-a0/experiments/datap-673/map-raw-to-actions.spec.ts`

## Score (2026-09-16 run)

| Rubric line | Result |
|-------------|--------|
| 1. re_ask_known | pass |
| 2. refuse_vs_invent | **fail** |
| 3. no_mush_merge | pass |
| 4. taxonomy_discipline | **fail** |
| 5. edge_mode | pass |
| 6. provenance | pass |

**Overall:** fail (valid result — model invented boundary/repo map and used non-enum taxonomy values).

See `score-report.json` for violation details.

## Durable new knowns (KB follow-up — not written into brief fixtures)

Candidate interview answers for DPKB later (from scripted stakeholder script, not fixture pins):

- **system_identity:** DataParade; in_scope = customer-facing app in scan-seed repo
- **Actors:** `cmp_6`/`cmp_20` → `persona`; `cmp_17`/`cmp_25` → `person`
- **`sends_data_to.data_categories` + `purpose`:** per `flow_*` in stakeholder script

Fixture pins unchanged until an explicit manifest bump PR.
