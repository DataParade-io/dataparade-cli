{
  "schema_version": 1,
  "event_id": "e53f88d4-7fbe-4fe3-a3be-c7190eb51388",
  "issue_id": "DATAP-602",
  "event_type": "field_updated",
  "occurred_at": "2026-08-26T20:11:17.266Z",
  "actor_id": "ryan",
  "payload": {
    "changes": {
      "description": {
        "from": "Build a standalone evaluation system that scores the dataparade-cli scanner's detection across all layers of its pipeline. The system computes precision, recall, and label accuracy as three separate scores (never averaged) and runs without LLM (deterministic mode only).\n\nThe scanner currently produces three layers of output:\n1. Components — services detected by pattern matching (routes, databases, APIs, auth). These become nodes in the architecture graph.\n2. Data flows — connections between components. These become edges.\n3. PII signal rules — lexical regex hits for sensitive data (SSN, email, card numbers), currently used only to enrich data flows, gated behind AI flags.\n\nEach layer needs its own evaluation with the same three-score framework but different cases and adapters. The scoring engine is shared; only the adapter (mapping layer output to findings) and the case set differ per layer.\n\nThe first PR delivers the shared scoring engine, types, and the Layer 1 (component detection) evaluation with a few fixture cases proving the system works — at least one passing and one failing assertion per score type. Zero scanner changes. Future PRs add Layer 2 (data flows) and Layer 3 (PII signals), the latter requiring a minimal scanner change to extract PII regex matching from the AI-gated path into a deterministic pass.\n\nThis issue tracks the evaluation system work. The experimental BDD work (branch DATAP-602-BDD) was the brainstorming phase; this is the production phase — clean PRs against main, no Cucumber, no research-metadata fields in the data, no external repo cloning.",
        "to": "Build a deterministic evaluation system for scanner quality across components, data flows, PII signals, and future data-item output.\n\nThe system uses two evaluation tiers:\n\n1. Fast committed fixtures for CI. These prove scorer and adapter behavior and provide regression coverage.\n2. A versioned benchmark of pinned third-party repository scopes. Humans review every scanner-relevant declaration or finding within each annotated scope so unmatched scanner output can be counted as false positives and missed ground truth can be counted as false negatives.\n\nMetrics are reported separately and never averaged:\n- Recall: matched positive ground truth divided by all evaluable positive ground truth.\n- Label accuracy: correctly labelled matches divided by matched positives.\n- Correct-label recall: correctly labelled matches divided by all evaluable positives.\n- Precision: true positives divided by all scanner findings inside exhaustively annotated scopes.\n- Negative-case pass rate: explicitly annotated negative cases left clean. This is a regression guardrail, not a substitute for precision.\n- Unread or unsupported cases are reported separately using exact per-file scan coverage.\n\nEach output layer has an adapter that produces canonical finding identity, labels, and evidence. Matching must consider layer-specific identity as well as normalized file and line overlap. The shared scorer is pure and stable but may evolve through reviewed contract changes.\n\nGround truth is versioned independently from scanner code. Agents may locate candidates and propose annotations, but humans approve new or changed labels. Corrections require source evidence, rationale, review state, and a separate corpus delta so benchmark changes cannot be mistaken for scanner improvements.\n\nThe first implementation PR delivers the shared evaluator and component smoke coverage without scanner changes. Follow-up work adds the curated benchmark, data-flow evaluation, and deterministic PII matching. PII rule extraction is separated from any decision to expose new findings in default product output.\n\nThe experimental DATAP-602-BDD branch remains research input. Production work removes Cucumber and scratch artifacts while preserving useful pinned-repository provenance, adversarial cases, measured baselines, and unsupported-file handling."
      }
    }
  }
}